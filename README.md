### Issue 1: PDF Doesn't Match Preview Template (URGENT)
Problem: Preview shows retail template with "Store Information" and "Return Policy" sections, but PDF shows completely different generic design.

Required Fix: PDF must be IDENTICAL to the preview design
- ✅ If preview shows "SALES INVOICE" header → PDF must show "SALES INVOICE"  
- ✅ If preview shows "Store Information" section → PDF must have "Store Information" section
- ✅ If preview shows "Return Policy" → PDF must include "Return Policy"
- ✅ Colors, layout, sections must be 100% identical between preview and PDF

### Issue 2: Logo Missing in PDF (URGENT)
Problem: Logo appears in invoice preview but disappears in exported PDF.

Required Fix:
- Logo must appear in PDF exactly where it shows in preview
- Same size, same position, same quality
- Support PNG, JPG, SVG formats in PDF

### Issue 3: Currency Selection Missing (HIGH PRIORITY)
Problem: App is hard-coded to USD ($) but needs global currency support.

Required Fix: Add currency selector with these currencies:
